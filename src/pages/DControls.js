import { useEffect, useState } from "react"
import Axios from 'axios'
import { useNavigate } from "react-router-dom"

const DControls = () => {
    const [devices, setDevices] = ([])

    let navigate = useNavigate()
    const showDevices = (posts) => {
        navigate
    }

    return (
        <div>
            
        </div>
    )
}

export default DControls